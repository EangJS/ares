# Running the PCTOOL

1. Copy this `pctool` folder to the linux test PC
2. Run `docker build -t timeserver-app .`
3. Run `./run.sh` (Ensure `run.sh` is executable first)

## Notes

Please connect the test PC to same wifi network as the DUT
